// main();

// function saveThePrisoner(n: number, m: number, s: number): number {
//   return (m + (s - 1)) % n == 0 ? n : (m + (s - 1)) % n;
// };

// function main() {
// 	const prisoners: number = 5;
// 	const sweets: number = 4;
// 	const initialChair: number = 5;

// 	const result : number = saveThePrisoner(prisoners, sweets, initialChair);

// 	console.log("Output:\n\n", result);

// 	console.log("\n- - -");
// };